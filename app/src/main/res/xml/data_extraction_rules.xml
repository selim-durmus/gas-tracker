<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ data extraction rules (referenced via android:dataExtractionRules).
  Specifying <include> restricts both cloud backup and device-to-device transfer to
  the Room database and shared prefs; everything else (cache, external storage) is
  excluded automatically.
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
